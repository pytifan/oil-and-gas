Start the Vite dev server for the React frontend with hot-reload.
Proxies /api to http://localhost:8080 (gateway must be running).
Opens at http://localhost:5173

!cd /Users/oleksiiprokopenko/workspace/calculations-frontend && npm run dev
